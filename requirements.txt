# --- OCR (Step 1) ---
# Pick ONE of these two. The code auto-detects whichever is installed.
paddleocr>=2.7        # matches the diagram ("OCR (PaddleOCR)")
paddlepaddle>=2.5
# pytesseract>=0.3.10   # lighter fallback alternative (uncomment if you use this instead)

Pillow>=10.0           # image loading/handling

# --- Knowledge base + retrieval (Steps 3-5) ---
scikit-learn>=1.3      # TF-IDF vector index + cosine-similarity retriever
numpy>=1.24

# Optional upgrade path to match the diagram exactly (FAISS / ChromaDB):
# faiss-cpu>=1.7.4
# chromadb>=0.4
# sentence-transformers>=2.2   # for real embedding vectors instead of TF-IDF

# --- Vision-language model (Step 7) ---
openai>=1.0            # works for GPT-4.1 / GPT-4.1 Vision, and any
                        # OpenAI-compatible endpoint (e.g. a hosted Qwen2.5-VL)

# --- Misc ---
requests>=2.31
